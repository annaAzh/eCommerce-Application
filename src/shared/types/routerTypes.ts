export enum Paths {
  start = '/',
  main = 'main',
  login = 'login',
  registration = 'registration',
  catalog = 'catalog',
  product = 'catalog/:productKey',
  profile = 'profile',
  notFound = '*',
}
